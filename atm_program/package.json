{
  "name": "atm_program",
  "version": "1.0.0",
  "description": "Write an ATM program that has the following functionalities. \tLogin with password to start the program.  \tCheck existing Account balance. \tAbility to Top-Up your account.  \tAbility to withdraw a certain amount. \tCreate a Button to Exit the program.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "watch:css":"yarn tailwindcss -i ./css/styles.css -o ./dist/styles.css --watch"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "tailwindcss": "^3.1.8"
  }
}
